import sys
sys.stdin = open("input.txt", "r")

def DFS(L):
    if(L == N):
        for i in range(0,N):
            resTest[i] = #요소
        if(sizeof(list) = N):
            print()
            #프로그램 자체를 종료하기
    else:
        for i in range(1,N+1):
            if( i in isList):
                continue
            else:
                res.append()


if __name__ == "__main__":
    N, F=map(int, input().split())
    DFS(0)
    isList = [0]*(N+1)
    res = [0]*N
    resTest = [0]*N
