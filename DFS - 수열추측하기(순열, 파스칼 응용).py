import sys
sys.stdin = open("input.txt", "r")

def DFS(L):
    if(L == N):
        for i in range(0,N):
            list1[i] = 요소
        if(sizeof(list) = N):
            print()
            프로그램 종료
    else:
        for i in range(1,N+1):
            if( i in isList):
                continue
            else:
                list.append()


if __name__ == "__main__":
    N, F=map(int, input().split())
    DFS(0)
